#!/bin/python
from binaryTree import bst;

# Given a binary tree of integers, 
# find the maximum path sum between two nodes. 
# The path must go through at least one node, 
# and does not need to go through the root.

#there is an important question for me exactly 
# how do they define path? does it mean you can only go thru a node once? 
# well in that case there is always only one path to go? 
# then just find the sum of that path? 
def maxTreePath(Tree,node1,node2):
   
   
   
   

if __name__ == "__main__":
    Tree = bst(height=3, is_perfect=False)
    print(Tree)