#!/bin/python

# Given a word W and a string S, find all starting indices in S which are anagrams of W.

# For example, given that W is "ab", and S is "abxaba", return 0, 3, and 4.
#def isAnagramOf(word,pAnagram): 
 
def FindAnagramIndexes(word,string):
    anagramIndex = []
    currentIndex = 0
    # convert words string to HashSet
    # though you need a way to check for too many duplicates
    

if __name__ == "__main__":
    print(function())