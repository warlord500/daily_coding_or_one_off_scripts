
# Find the paths from root to leaves

# I guess my method would first find all leaves.

# Then trace path from root to leaf
# Once path is traced add it to overall list


def find_leaves(tree)
List ofleaves= [ ]
FindleavesR(tree,listOfLeaves)

Set find_leavesR(node,listOfLeaves)
if left = none & right = none
    Listof leavesR.add(node)
FindleavesR(node.left) 
FindLeavesR(node.right)

Trace path(value):
While node.value!= value
   If node.value 
